import * as THREE from 'three';
import { PhysicBody } from '../physics/PhysicBody';

export const drawForces = (scene: THREE.Scene, physics: PhysicBody) => {
    // const arrowHelper = new THREE.ArrowHelper()
    // scene.add(arrowHelper);
};
